# 自适应教学 Agent PPT 演示文档

## 一、演示定位

建议 PPT 控制在 8 页左右，现场演示控制在 8—10 分钟。

演示主线：

    学生回答
      ↓
    一次真实 LLM 调用
      ↓
    困难判断 + 教学动作 + 教师回复
      ↓
    状态保存与下一轮教学

## 二、PPT 页面内容

### 第 1 页：项目名称

标题：基于模型优先决策的自适应教学 Agent

副标题：物理与高等数学案例演示

开场说：

> 本项目不是固定问答，而是让模型根据学生每一轮的真实回答决定下一步怎么教。

### 第 2 页：要解决的问题

普通问答通常只负责回答问题，无法持续判断：

- 学生是概念错误，还是公式看不懂；
- 是需要解释、诊断，还是分层提示；
- 学生答对后能否迁移到新情境。

本项目保留教学上下文，并把学生状态变化记录下来。

### 第 3 页：系统架构

一轮实时教学只有一个主要模型调用，同时输出：

- 内容 Skill；
- 教学动作；
- 困难类型；
- 学习证据；
- 掌握度；
- 教师回复。

内容 Skill 负责“教什么”，教学动作负责“怎么教”。程序只校验 Skill、保存状态和处理终止边界。

### 第 4 页：统一教学动作

系统不再让多个策略 Skill 互相抢决策，只保留一个自适应教学 Skill，内部动作有五种：

- explain：内容讲解；
- diagnose：定位学生卡点；
- scaffold：逐步降低任务难度；
- correct：纠正明确的错误模型；
- transfer：换情境验证迁移。

### 第 5 页：物理案例

案例：公交车急刹车、惯性与运动状态变化。

演示路径：

    困惑 → 明确错误观点 → correct → transfer → success

现场依次输入：

    我不确定，物体没有受到水平方向的力，为什么还会继续向前运动？

    我认为只要物体在运动，就一定需要一个向前的力来维持运动。

    现在我明白了：合力为零时，物体会保持静止或匀速直线运动；惯性是保持这种状态的性质，不是力。

    公交车突然加速时，我会相对车向后仰，因为身体想保持原来的静止状态，而车向前加速。

    公交车向右转弯时，我会相对车向左偏，因为身体想保持原来向前的直线运动状态。

出现成功状态后停止，不必为了展示继续追加轮次。

### 第 6 页：导数案例

案例：从平均变化率逼近瞬时变化率，理解导数的极限定义。

现场依次输入：

    时间区间无限缩小。

    这个不会啊。

    我看不懂。

预期重点：

    scaffold → scaffold → scaffold

说明：

> “我看不懂”没有明确表达错误概念，所以模型应该继续拆小任务，而不是直接进入误解纠正，也不应该突然换成无关例子。

### 第 7 页：评估与回放

展示过程回放页面和 Agent 评估页面：

- 每轮保存学生回答、教师回复和状态前后变化；
- 保存内容 Skill、教学动作和调用审计；
- 支持物理和导数的真实 API 演示脚本；
- 评估页面支持多案例、多方法对比和结果导出。

评估结果用于验证决策逻辑和交互契约，不冒充真实课堂学习效果。

### 第 8 页：总结

总结句：

> 项目的核心不是堆叠更多 Skill，而是让大模型真正参与每一轮教学决策。物理案例展示误解纠正和迁移，导数案例展示面对符号困难时的分层提示。

## 三、现场启动

在 PowerShell 中执行：

    cd D:\桌面\华师智能教育考核\考核2
    streamlit run streamlit_app.py --server.port 8501

面试前先运行：

    python scripts\demo_physics.py
    python scripts\demo_derivative.py

两个脚本必须使用真实 API；如果 API 不可用或发生离线回退，脚本会直接失败。

## 四、现场展示顺序

1. 打开实时教学页，确认 LLM 已连接；
2. 选择物理预设，展示内容 Skill 和教学动作；
3. 输入物理三到五轮回答，展示 correct 和 transfer；
4. 重新开始导数预设，输入三轮回答，展示连续 scaffold；
5. 打开过程回放，展示状态和调用记录；
6. 打开评估页面，展示评估结果；
7. 用《面试题文档.md》回答老师追问。

## 五、演示注意事项

- 不要手动切换教学策略；
- 只选择案例对应的内容 Skill；
- 不要把每次生成的具体文字当成固定答案；
- 重点展示教学动作、困难识别和状态变化；
- 不设置 TEACHING_AGENT_OFFLINE=1；
- 如果模型提前达到成功条件，直接说明终止规则已生效。
