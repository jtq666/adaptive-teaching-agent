# 教学 Agent 答辩演示文档

## 一、演示目标

本次演示不要求切换所有学科内容 Skill。一个物理案例会保持“牛顿第一定律”内容 Skill 不变，同时让统一自适应教学 Skill 根据学生原话选择不同教学动作：

```text
diagnose → scaffold → correct → transfer
```

这正是项目中“内容 Skill 稳定、教学动作自适应”的设计。页面展示的是一个统一的
`adaptive_teaching_v1`，而不是四个互相抢决策权的策略 Skill。

## 二、演示前准备

1. 启动项目：

   ```powershell
   streamlit run streamlit_app.py --server.port 8501
   ```

2. 打开 `http://localhost:8501`。
3. 新建一个教学对话，不要接着使用旧会话。
4. 选择大学物理、牛顿第一定律；如果页面提供学生水平，选择“基础薄弱”。
5. 确认使用真实 API，不要设置 `TEACHING_AGENT_OFFLINE=1`。
6. 每次只提交一条学生回答，等教师回复后再提交下一条。

## 三、物理完整演示脚本

开始后，教师会自动给出公交车急刹车情境和第一个问题。下面只需要依次输入“学生回答”。

### 第 1 轮：触发诊断提问

输入：

```text
我不知道，完全不明白为什么公交车刹车时人会向前倾。
```

预期观察：页面显示 `内容 Skill：newtons_first_law_via_engineering_examples_v1`，教学动作可能为
`diagnose` 或 `explain`。真实模型可因上下文略有差异，重点是它先定位困难，不直接纠正。

教师会先定位学生到底卡在“运动状态、力，还是惯性”中的哪一个部分，不会直接讲完整答案。

### 第 2 轮：触发分层提示

输入：

```text
我还是不知道，物体没有受到力为什么还能继续运动？
```

预期观察：教学动作显示为 `scaffold`，回复只给一个更小的下一步，不直接给完整答案。

这一轮教师应该把问题拆成一个更小的判断，只给一个提示，让学生完成下一步。

### 第 3 轮：触发误解纠正

输入：

```text
我仍然不懂，我觉得没有力物体就不可能一直运动。
```

预期观察：教学动作显示为 `correct`，回复对比“力维持运动”和“力改变运动状态”两个模型。

这一轮教师会把“力维持运动”的旧说法和“力改变运动状态”的正确原则进行对比，通常还会给出一个最小反例。

### 第 4 轮：纠错后给出正确解释

不要继续重复“我不会”，否则系统可能判断纠错后仍然没有改善并结束会话。输入：

```text
现在我明白了：物体不受合力时会保持原来的静止或匀速直线运动状态；惯性是保持这种状态的性质，不是力。
```

预期观察：教学动作回到 `explain` 或 `diagnose`，内容 Skill 仍是
`newtons_first_law_via_engineering_examples_v1`。

这表示误解已经改善，系统回到物理内容本身继续推进。

### 第 5 轮：完成刹车受力解释

输入：

```text
刹车时车受到向后的制动力，速度改变说明有向后的加速度，合力不为零；人的身体相对车前倾，是因为身体想保持原来向前的运动状态。
```

预期观察：

教师会继续围绕“合力与运动变化”推进，并可能完成当前学科知识点。

### 第 6 轮：触发迁移验证

输入：

```text
速度发生变化说明合力不为零；脚底和地板的摩擦力使下半身随车减速，上半身暂时保持原来的运动状态。
```

预期观察：教学动作显示为 `transfer`，教师更换表面情境，但保持同一个知识目标。

教师会换一个表面不同但原理相同的新情境，而不是继续重复公交车刹车原题。

### 第 7 轮：完成迁移回答

如果教师改问公交车转弯，输入：

```text
如果公交车突然向右转，我会相对车向左，也就是向转弯外侧偏，因为身体保持原来直线运动状态。
```

预期观察：

```text
迁移证据：transfer
```

看到迁移证据后即可停止演示，不必继续输入。

如果继续输入，迁移验证已经通过但整体掌握度尚未达到终止阈值时，系统会回到掌握度最低的知识点进行一次学科巩固，不会重复刚才的电梯题。这是正常的自适应行为。

## 四、答辩时重点展示什么

每次教师回复下方都会显示本轮的内容 Skill、教学动作和阶段。建议重点展示：

1. 学生困惑时，教学动作从讲解转为诊断或分层提示。
2. 第 3 轮教师不只是说“你错了”，而是对比错误模型和正确原则。
3. 第 4 轮学生答对后，策略回到学科内容教学。
4. 第 6 轮换到新情境，出现 `transfer`。
5. 点击某一轮下方的“本轮教学证据”，展示系统为什么切换策略。

## 五、演示中容易出错的地方

- 不要一开始就连续输入正确答案，否则系统会直接推进内容 Skill，看不到支架和纠错。
- 不要在第 3 轮之后继续连续说“我还是不会”，连续纠错失败会触发“无法继续学习”的终止状态。
- 不要在同一会话中反复重试。没有触发预期 Skill 时，新建会话后从第 1 轮重新开始。
- 学科内容 Skill 不会为了演示而切换成别的学科；物理案例中保持牛顿第一定律是正常的，真正变化的是统一自适应 Skill 的教学动作。

## 六、导数备用案例

如果答辩时改演示高等数学，可以新建“导数”教学对话，依次输入：

```text
我不明白，导数为什么不是一个很小的数？
```

```text
我还是不知道，取0.01秒的平均变化率不就是瞬时速度吗？
```

```text
我仍然不懂，我觉得只要时间间隔足够小，就不需要极限。
```

预期会看到内容 Skill 仍保持导数主题；教学动作通常会从讲解转为分层提示。若学生明确表达“导数就是取一个固定很小的数”，模型才适合选择 `correct`，不要把单纯的“我看不懂”当成误解。

```text
explain/diagnose → scaffold → correct（仅在明确错误观点时）
```

之后给出正确解释，例如：

```text
我明白了：导数不是取一个固定的小数，而是让时间间隔趋近于零时，平均变化率的极限。
```

系统就会回到导数内容 Skill，继续推进后面的应用或迁移。
