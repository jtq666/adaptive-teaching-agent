# 教学 Agent 答辩演示脚本

这份文档对应两个已经写入项目的真实 API 脚本：

```powershell
python scripts\demo_physics.py
python scripts\demo_derivative.py
```

两个脚本都会拒绝离线回退。现场按下面的学生回答逐条输入即可；教师每次回复后再输入下一条。

## 一、演示前准备

```powershell
cd D:\桌面\华师智能教育考核\考核2
streamlit run streamlit_app.py --server.port 8501
```

确认侧栏显示“LLM 已连接”，新建会话并选择对应预设。预设已经默认限制到对应内容 Skill：

- 牛顿第一定律：`newtons_first_law_via_engineering_examples_v1`
- 导数极限定义：`derivative_intro_via_slope_limit_v1`

如果手动修改过“高级设置”，请把知识点保留为预设中的单个完整目标，并重新选择对应内容 Skill。不要设置 `TEACHING_AGENT_OFFLINE=1`。

## 二、物理完整演示

主题：公交车急刹车与惯性。教师首轮自动生成问题后，依次输入：

### 1. 学生困惑

```text
我不确定，物体没有受到水平方向的力，为什么还会继续向前运动？
```

此轮可能是 `explain` 或 `scaffold`，重点是内容 Skill 保持牛顿第一定律，模型开始承接困惑。

### 2. 明确错误观点

```text
我认为只要物体在运动，就一定需要一个向前的力来维持运动。
```

必须观察到教学动作 `correct`。展开“本轮教学证据”，展示错误模型与正确原则的对比。

### 3. 学生修正

```text
现在我明白了：合力为零时，物体会保持静止或匀速直线运动；惯性是保持这种状态的性质，不是力。
```

模型应回到 `explain` 或进入 `transfer`，不能继续把这句话判成需要纠正的错误。

### 4. 迁移情境

```text
公交车突然加速时，我会相对车向后仰，因为身体想保持原来的静止状态，而车向前加速。
```

必须观察到 `transfer`。若教师继续追问转弯，再输入：

```text
公交车向右转弯时，我会相对车向左偏，因为身体想保持原来向前的直线运动状态。
```

若页面已经显示“教学完成”，立即停止；若仍是进行中，再输入：

```text
电梯突然向上加速时，我会感觉被压向地板，因为身体想保持原来的静止状态。
```

重点展示：同一个物理内容 Skill 没有被替换，但统一 `adaptive_teaching_v1` 从讲解/纠正切换到了迁移验证；每轮回复不超过 100 字。

## 三、导数完整演示

主题：从平均变化率理解导数的极限定义。教师首轮自动生成问题后，依次输入：

### 1. 先给出直觉

```text
时间区间无限缩小。
```

### 2. 暴露符号困难

```text
这个不会啊。
```

### 3. 明确仍然看不懂

```text
我看不懂。
```

必须观察到至少两轮 `scaffold`，并且不能进入 `correct`。教师应保持导数原情境，逐步缩小任务粒度，不重复换成公交车等无关例子。

重点展示“困难类型”和“本轮教学证据”：这是符号/任务理解困难，不是学生表达了明确的错误概念。

## 四、现场展示顺序

1. 先演示物理：展示 `correct → explain/transfer → transfer`。
2. 再新建导数会话：展示连续 `scaffold`，说明模型没有因为“我看不懂”就乱切换到误解纠正。
3. 点击任意一轮的“查看详细记录”，展示选择理由、困难识别、掌握度变化和下一关注点。
4. 进入“过程回放”查看完整 JSON 轨迹；进入“Agent 评估”运行快速评估。

## 五、真实 API 脚本验收

脚本会自动检查内容 Skill、统一自适应 Skill、单回合单次调用、回复长度、动作事件和是否发生离线回退，并把结果保存到：

```text
output\evaluations\demo_physics_run.json
output\evaluations\demo_derivative_run.json
```

如果真实 API 余额、网络或密钥失效，脚本会直接失败，不会伪造“演示通过”。
