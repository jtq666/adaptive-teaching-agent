# 自适应教学 Agent 面试演示流程

## 一、演示目标

本项目展示一个模型优先的自适应教学 Agent。演示时重点说明三件事：

1. 内容 Skill 负责当前学科知识；
2. 大模型根据学生原话自主判断困难，并选择教学动作；
3. 程序只负责保存状态、校验 Skill 合法性和判断是否结束。

项目没有把“我看不懂”“连续答错”等固定句子硬编码成策略。每一轮真实 API 调用同时生成学生理解判断、困难类型、教学动作和教师回复。

建议面试演示总时长：8—10 分钟。

## 二、演示前准备

在 PowerShell 中进入项目目录：

    cd D:\桌面\华师智能教育考核\考核2
    streamlit run streamlit_app.py --server.port 8501

浏览器打开：

    http://localhost:8501

确认页面能够连接真实 API。演示时不要设置 TEACHING_AGENT_OFFLINE=1。

正式打开页面前，建议先在终端运行两个真实 API 演示脚本：

    python scripts\demo_physics.py
    python scripts\demo_derivative.py

两个脚本都会拒绝离线回退，并检查：

- 返回的内容 Skill 是否正确；
- 是否使用统一的自适应教学 Skill；
- 是否确实发生了 explain、scaffold、correct、transfer 等教学动作；
- 每轮是否只有一次模型调用；
- 回复是否不超过约 100 字且最多包含一个问题；
- 物理案例是否能进入成功状态。

## 三、开场介绍（约 30 秒）

可以直接这样说：

> 这是一个面向物理和高等数学的自适应教学 Agent。系统不是把教学过程写成固定题库，而是把学生的真实回答交给大模型判断。模型会识别学生当前的困难，选择讲解、诊断、分层提示、误解纠正或迁移练习，并生成下一句教师回复。程序只负责保存状态和安全边界。

## 四、物理案例：惯性与运动状态变化（约 4 分钟）

### 4.1 选择案例

在首页选择物理课程，选择“惯性与运动状态变化”案例。

如果页面显示可选内容 Skill，保留：

    newtons_first_law_via_engineering_examples_v1

这是本案例的内容 Skill。教学动作仍由大模型根据学生回答决定，不要手动切换策略。

### 4.2 第一轮：表达困惑，触发理解诊断或讲解

题目出现后，在学生输入框中输入：

    我不确定，物体没有受到水平方向的力，为什么还会继续向前运动？

点击提交。

观察页面中的本轮信息：

- 内容 Skill：牛顿第一定律相关 Skill；
- 教学动作：通常为 explain 或 diagnose；
- 困难类型：通常为 concept_misconception 或 unknown；
- 回复应围绕“合力为零时保持原有运动状态”继续引导。

这里不要求每次生成完全相同的文字，重点看模型是否承接了学生的真实困惑，而不是机械复述公交车情境。

### 4.3 第二轮：明确表达错误观点，触发误解纠正

输入：

    我认为只要物体在运动，就一定需要一个向前的力来维持运动。

点击提交。

本轮应重点观察：

- 教学动作进入 correct；
- 回复指出“运动需要力维持”这一错误模型；
- 回复给出一个简短对比或反例；
- 回复最后只保留一个可继续回答的问题。

答辩时可以解释：

> 第一次只是困惑，第二次才明确说出错误观点，所以程序没有看到关键词就强行纠正，而是由模型根据学生原话识别出概念误解。

### 4.4 第三轮：修正后进入迁移

输入：

    现在我明白了：合力为零时，物体会保持静止或匀速直线运动；惯性是保持这种状态的性质，不是力。

点击提交。

答对后，模型应减少纠错，逐渐进入 transfer 或继续用内容 Skill 讲解。

### 4.5 第四至第七轮：公交车和电梯迁移

依次输入以下回答，观察场景变化和迁移动作：

    公交车突然加速时，我会相对车向后仰，因为身体想保持原来的静止状态，而车向前加速。

    公交车向右转弯时，我会相对车向左偏，因为身体想保持原来向前的直线运动状态。

    电梯突然向上加速时，我会感觉被压向地板，因为身体想保持原来的静止状态。

    不会。电梯匀速上升时合力为零，身体保持匀速运动，不会因为加速而有额外的压迫感。

当页面显示成功或掌握度达到结束条件时停止。若模型提前判断已经掌握，也可以停止并说明：系统的终止判断由状态掌握度控制，不需要为了展示而强行多问。

### 4.6 物理案例要讲出的结论

这组输入通常能够展示：

    explain / diagnose → correct → transfer → success

重点不是动作名称必须固定顺序，而是：

- 明确误解后才进入纠正；
- 正确后能够迁移到加速、转弯和电梯；
- 不是每轮都重复相同问题；
- 内容 Skill 保持稳定，教学动作根据学生状态变化。

## 五、导数案例：从平均变化率到导数定义（约 2 分钟）

### 5.1 选择案例

重新开始一个学习会话，选择高等数学中的“导数的极限定义”案例。

保留内容 Skill：

    derivative_intro_via_slope_limit_v1

### 5.2 按顺序输入三轮回答

第一轮输入：

    时间区间无限缩小。

预期：模型先用内容 Skill 继续讲解平均变化率逼近瞬时变化率。

第二轮输入：

    这个不会啊。

预期：进入 scaffold，任务粒度降低，但不要直接把完整极限公式全部给出。

第三轮输入：

    我看不懂。

预期：仍然使用 scaffold，并进一步只给一个更小的步骤，例如先辨认分子或两个时间点。因为“我看不懂”没有明确表达错误概念，不应直接进入 correct。

答辩时可以说：

> 导数案例专门验证系统能区分“符号看不懂”和“概念理解错误”。学生说“我看不懂”时，模型应该降低任务粒度，而不是随意换成另一个例子或直接泄露完整答案。

### 5.3 导数案例要讲出的结论

这一组输入的核心观察结果是：

    scaffold → scaffold → scaffold

困难类型通常为 symbol_notation 或 task_comprehension。回复可以不同，但不能因为同一句“我看不懂”就机械跳到误解纠正。

## 六、界面展示顺序（约 1 分钟）

每提交一轮真实回答后，按以下顺序展示页面：

1. 学生原话和教师回复；
2. 内容 Skill；
3. 教学动作；
4. 困难类型和本轮教学证据；
5. 如需说明，再展开详细信息；
6. 展示掌握度、当前关注点和下一步关注点；
7. 展示会话是否继续或已经结束。

前端默认只显示重要信息，详细决策信息放在折叠区域，避免把内部日志当成教学对话展示。

## 七、如何解释系统架构

可以用下面这段话回答：

> 每轮只有一个主要模型调用。这个调用同时完成状态理解、困难识别、教学动作选择和教师回复生成。内容 Skill 与教学动作分开保存：内容 Skill 说明教什么，教学动作说明怎么教。程序不再根据失败次数、关键词或轮数覆盖模型决策，只检查内容 Skill 是否存在、动作是否合法、状态是否需要结束。

统一教学动作只有五种：

- explain：内容讲解；
- diagnose：诊断学生卡点；
- scaffold：分层降低任务难度；
- correct：纠正明确的错误模型；
- transfer：换情境验证迁移。

## 八、老师可能追问的问题

### 1. 为什么不保留很多独立策略 Skill？

因为多个策略模块会互相覆盖决策，容易出现 Skill 乱跳。现在只保留一个自适应教学 Skill，内部使用五种教学动作，内容 Skill 单独负责学科知识。

### 2. 这是不是硬编码？

案例的内容 Skill 和演示输入是预先准备的，但“学生回答后采用什么教学动作、困难是什么、教师如何回复”由一次真实 API 调用生成。代码没有把固定学生句子映射到固定动作，也没有用关键词决定策略。

### 3. 程序还做什么？

程序负责读取配置、传递当前上下文、检查 Skill 合法性、保存模型返回的状态、记录调用审计，并在掌握度达到条件或发生异常时结束会话。

### 4. 为什么演示时要选择指定内容 Skill？

因为面试案例已经确定教学主题。指定的是“教什么”，不是“怎么教”。在这个内容范围内，模型仍然自主判断学生困难和教学动作。

### 5. 为什么导数中的“我看不懂”使用分层提示？

它只表明学生当前无法理解，没有明确指出错误观点。合理动作是把任务拆小；只有学生明确坚持错误概念时，才适合使用误解纠正。

### 6. API 暂时不可用怎么办？

项目保留离线回退用于结构回归，但正式演示脚本会检测回退标志并直接报错，因此不会把离线结果冒充真实 API 演示。面试前先运行两个演示脚本即可确认余额和接口状态。

## 九、结束陈述（约 30 秒）

可以这样收尾：

> 这个项目的重点不是堆叠很多 Skill，而是让大模型真正参与每一轮教学决策。物理案例展示了从困惑、误解纠正到跨情境迁移；导数案例展示了面对符号困难时逐步降低任务粒度。两类案例共用同一套自适应框架，同时保留了学科内容 Skill 的差异。

## 十、面试前最终检查清单

- [ ] streamlit_app.py 可以正常启动；
- [ ] 真实 API 余额和模型配置正常；
- [ ] python scripts\demo_physics.py 通过；
- [ ] python scripts\demo_derivative.py 通过；
- [ ] 物理和导数案例的内容 Skill 已准备；
- [ ] 关闭或不设置 TEACHING_AGENT_OFFLINE=1；
- [ ] 浏览器页面只展示重要信息；
- [ ] 不在演示中手动切换教学策略；
- [ ] 物理演示重点展示 correct 和 transfer；
- [ ] 导数演示重点展示 scaffold；
- [ ] 遇到生成文字不同，解释“模型输出允许有自然变化，验收看教学行为和状态变化”。

## 十一、相关文件

- README.md：项目启动和结构说明；
- 项目文档报告.md：提交给老师的项目报告；
- 教学演示文档.md：两个案例的详细输入脚本；
- 答辩演示流程.md：简版答辩流程；
- scripts\demo_physics.py：物理真实 API 验收脚本；
- scripts\demo_derivative.py：导数真实 API 验收脚本；
- output\evaluations\demo_physics_run.json：物理最近一次运行记录；
- output\evaluations\demo_derivative_run.json：导数最近一次运行记录。
