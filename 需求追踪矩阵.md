# 题目二需求追踪矩阵

本矩阵将题目原文、运行界面、持久化证据与自动化测试对应，现场答辩可据此逐项展示。

| 题目要求 | 实现证据 | 界面证据 | 测试证据 | 状态 |
|---|---|---|---|---|
| 输入教学目标、学生画像、当前掌握状态、历史对话和 Skill 列表 | `TeachingGoal`、`StudentProfile`、`StudentState`、`TeachingSession`、`SkillLibrary` | 实时教学表单、回放继续教学 | `test_start_session_selects_subject_and_diagnostic_support`、前端表单测试 | 已满足 |
| 每次只输出下一步动作，等待真实回答 | `handle_student_message` 每次只追加一轮 | `st.chat_input` 实时输入 | `test_live_teaching_form_and_four_stage_chat` | 已满足 |
| 单 Teaching Agent 内部职责清晰且保持统一状态 | 对外一个 `HybridTeachingAgent`；实时回合由一次自适应模型输出同时完成理解判断、内容 Skill、教学动作和回复，统一保存 `TeachingSession` | 每轮展示内容 Skill、教学动作和简短证据 | `single_llm_adaptive_turn` 审计、单次调用真实 API 验收 | 已满足 |
| 教学目标在多轮中稳定推进 | 会话保存稳定 `TeachingRoute`；只有当前知识点出现匹配证据才推进，最终仍要求掌握度和迁移验证 | 实时教学同一页面显示当前目标、路线进度和每轮证据 | 路线生成、越界知识点拒绝、证据不足不推进、匹配证据推进测试 | 已满足 |
| 每轮保持上下文连续 | 单次模型输入包含目标、学生画像、状态、上一轮回复、微步骤和学生原话；程序保存每轮上下文证据 | 同一条对话显示自然教师话语与内容 Skill/教学动作 | 微步骤持久化、短回答复用情境、真实导数/物理路径验收 | 已满足 |
| 回答交互清晰且可实时提交 | 当前基础版新会话固定开放回答，旧会话题型数据保持可迁移和回放 | `st.chat_input` 作为唯一主回答入口 | 开放回答新会话、真实提交、旧选择题会话兼容测试 | 已满足（主动收敛范围） |
| Skill 选择、组合和动态切换 | 模型从 Skill Library 候选中选择内容 Skill；统一 `adaptive_teaching_v1` 内部选择五种教学动作，内容与动作分开记录 | 每轮展示“内容 Skill + 教学动作”和切换理由 | 内容 Skill 合法性、跨课程候选、真实动作切换验收 | 已满足 |
| 成功或无法继续时终止 | 掌握阈值 + 迁移验证；轮次/停滞守卫 | 状态 Badge、终止依据、回放轨迹 | 成功、最大轮数、重复误解测试 | 已满足 |
| 显式维护四类学生状态 | 掌握度证据估计、误解、理解信号、下一关注点；另有逐知识点证据和置信度，未映射知识点不猜测加分 | 侧栏掌握度、误解、状态证据 | `test_state_evidence_only_changes_affected_point`、未映射知识点和证据可达性测试 | 已满足 |
| 每轮显示 Skill 与理由 | `ConversationTurn` 保存选择理由、候选和规则 | 每条教师消息下方显示“内容 Skill × 教学策略 × 教学动作”，证据按轮展开 | AppTest 四阶段聊天 | 已满足 |
| 可复现完整过程与结果评估 | 6 开发 + 12 独立冻结留出案例、3 方法、3 画像、5 种子、案例聚类配对统计 | 快速/完整评估、统计检验、逐案例 | 留出集独立性、方法盲模拟器、共享阈值、案例聚类测试 | 已满足 |
| 至少一个基线 | 固定单 Skill、无 Skill 通用教师 | 结果总览对比 | `test_evaluation_has_eighteen_cases_and_two_baselines` | 已满足（实际 2 个） |
| 状态、决策、行为和效果四类指标 | F1/MAE、Skill/切换/终止、八维 proxy、前后测/迁移 | 五个评估视图和报告导出 | 评估不变量测试 | 已满足 |
| 成功与失败案例分析 | 自动选择典型成功/失败案例 | 结果总览案例卡片 | Markdown 报告测试 | 已满足 |
| 完整代码、环境、依赖、配置和 API 说明 | README、`.env.example`、锁定依赖、settings | 侧栏显示在线/离线状态 | 编译、Ruff、pytest、密钥扫描 | 已满足 |
| 现场连续演示五项要求 | 预置任务、演示回答建议、Skill Library、回放和快速评估 | 四页连续操作 | AppTest + Playwright 真实浏览器验收 | 已满足 |

## 当前验收记录

- 核心自动化回归：150 项；覆盖 Agent 编排、实时教学链路、状态更新、Skill 切换、教学路线、评估公平性、会话存储和主要页面。测试数量只是工程回归记录，不是题目要求。最近一次结果为 `150 passed`。
- 静态检查：Ruff 与 mypy 全部通过；依赖锁文件可复现当前运行环境。
- 浏览器验收：真实 API Streamlit 服务的 Chromium 中完成新建、开放回答提交、Skill 切换、证据展开、Skill 检索、会话改名/复制/归档、快速评估和导出入口检查；另完成 1366×768、1920×1080、390×844 截图复核。删除/恢复、导入冲突等完整 CRUD 由 AppTest 覆盖。
- 真实 API 验收：项目提供 `scripts/online_adaptive_teaching_acceptance.py`，强制要求真实 API，不接受离线回退；覆盖导数连续分层提示、物理明确误解纠正、内容 Skill 合法性、单回合单调用和 100 字回复边界。历史语义、路线和多案例脚本继续保留用于兼容回归，不作为本次实时架构的决策链。
- 答辩脚本验收：`scripts/demo_physics.py` 与 `scripts/demo_derivative.py` 已在真实 `deepseek-chat` 下跑通；物理触发纠正、迁移并成功终止，导数触发连续分层提示，均保存脱敏 JSON 轨迹。
- 覆盖率验收：删减非核心边界回归后最近一次总覆盖率为 86.30%；`pyproject.toml` 保留 85% 基础健康线。覆盖率和测试数量均为工程参考，不是题目硬性要求。
- 视觉视口：1366×768、1920×1080、390×844 均完成截图复核；窄屏使用可收起导航抽屉，主内容不会横向挤坏。
- 性能验收：1000 会话索引分页查询约 9.7 ms，500 报告归档列表约 15.8 ms，测试均使用临时目录。
- 交付验收：最终规范包由 `scripts/build_submission.ps1` 生成，白名单 ZIP 使用 schema v6 会话、完整评估和真实 API 验收证据；`.env`、缓存和字节码文件不进入交付包。ZIP 条目数与 SHA-256 写入 ZIP 同级的外部校验清单。

## 结论边界

- 自动行为评分是规则代理，不冒充人工教学质量结论。
- 当前人工盲评协议和表格已提供，但真实双评审标注仍为“待完成”。
- 留出集使用独立文件和不同教学情境，不再由运行时代码克隆开发集；但仍由同一项目团队构建，不能替代跨教师独立命题集或真实学生实验。
