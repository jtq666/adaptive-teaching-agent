# 第二轮考核：评委关注点与回答证据

## 最值得重点展示的七项能力

| 评委可能关注 | 不要只讲什么 | 现场可核验证据 |
|---|---|---|
| 是否真的是多轮 Agent | 不只展示一段漂亮对话 | 每次学生提交后才更新状态并生成唯一下一步；顶部四段决策链 |
| 是否理解学生差异 | 不只展示学生画像表单 | 掌握度、误解标签、原回答证据、累计次数、理解信号和下一关注点 |
| Skill 是否真正参与决策 | 不只报最终 Skill 名称 | 候选列表、主辅 Skill、LLM/规则决策来源、选择与切换理由 |
| 是否会动态调整 | 不只重复同一种讲法 | 连续错误依次触发诊断、分层提示、误解纠正，掌握后触发迁移验证 |
| 是否可靠可控 | 不只依赖一次 LLM 输出 | 结构化 JSON 校验、重试、候选约束、规则回退、直接答案输出守卫 |
| 是否知道何时停止 | 不把“说得像懂了”当成功 | 全知识点达到阈值且迁移通过才成功；8 轮或误解停滞则暂停并建议后续路径 |
| 评估是否可信 | 不只展示一张平均分图 | 6 开发 + 12 留出、3 模拟画像、5 共享种子、同 8 轮预算；逐案例轨迹可导出复核 |

## 建议主动讲出的结论

第一题解决“如何得到可执行 Teaching Skill”，第二题的核心是“面对这个学生的这一次回答，下一轮应该执行哪个 Skill”。本项目的关键产物不是聊天界面，而是可解释、可回放、可终止、可量化比较的教学决策闭环。

## 面试展示优先级

如果时间只有 8–10 分钟，建议按下面顺序，不要从页面美观或代码目录讲起：

1. **实时性**：输入一条现场回答，证明系统只生成一个下一步，而不是播放预设脚本。
2. **动态性**：连续给出困惑回答，展示 `学科讲解 → 诊断 → 分层提示 → 误解纠正` 的可达状态机。
3. **可解释性**：展开候选审计，指出哪些 Skill 被硬过滤、规则分、最终约束和回答证据。
4. **终止严谨性**：说明“掌握度达到阈值”仍不够，必须通过独立迁移验证。
5. **评估可信度**：主动展示固定 Skill 在迁移率等指标上可能更高；当前模拟中自适应方法主要在决策质量、标准化增益和单位轮次效率上占优，不声称全面获胜。
6. **工程稳健性**：最后再展示回放、恢复删除、离线模式、150 项自动化测试和覆盖率质量门槛。

## 最可能被追问的薄弱点

| 追问 | 应直接承认的边界 | 已做的控制 / 下一步 |
|---|---|---|
| 18 个案例是否真的独立 | 12 个留出案例已改为单独冻结的新情境，不由开发案例运行时克隆；但仍非外部独立采集 | 开发/留出分栏、唯一 ID 与标签冻结；下一步做跨教师独立命题集 |
| 模拟学生能否代表真实学生 | 不能证明真实课堂因果效果 | 3 画像 × 5 种子用于回归稳健性；下一步真实学生随机对照与延迟测验 |
| 八维教学质量是不是自说自话 | 当前自动分是 proxy | 已输出盲评表和解盲密钥；双评分者 κ 未完成时明确标“待完成” |
| 掌握度为什么可信 | 它是证据驱动估计，不是心理真实值 | 每个变化绑定原句、知识点、置信度；模拟中用隐藏真值算 MAE |
| LLM 是否真正必要 | 规则链本身可独立运行；LLM只处理语义歧义 | 在线/离线并存，LLM 不能越过候选约束和终止守卫 |
| 为什么某些指标固定 Skill 更高 | 重复同一动作可能在特定迁移题上形成稳定练习，但教学决策并不自适应 | 同时报共同轮次、自然终止、单位轮次效率与迁移率；不隐藏这一 trade-off |

## 高频追问与回答

### 为什么新增四个 Skill？

原有 10 个 Skill 主要回答某个学科主题“怎么教”，缺少跨学科的诊断、受阻补救、误解纠正和终止验证。新增 Skill 是控制层能力，可与学科 Skill 组合，不替代第一题成果。

### LLM 选错或接口不可用怎么办？

LLM 只能从课程、目标、触发条件和前置条件过滤后的候选集合中选择。输出须通过结构化校验；非法 JSON、超时或无 API 时按规则排序回退。连续错误与终止条件由确定性守卫执行，不交给 LLM 自由决定。

### 如何证明不是直接给答案？

提示和纠错 Skill 明确要求保留学生思考步骤；输出守卫拦截典型直接答案表达，并保证教师话语以可回答问题结束。评估报告同时记录直接答案违规率。

### 学生掌握度是真实的吗？

实时场景中它是基于回答证据的可更新估计，不宣称等于心理真实状态。评估场景另外保存模拟学生的隐含真实状态，用掌握度 MAE、误解 F1 和关注点准确率检验估计质量。

### 基线是否被故意做弱？

三种方法共享案例、画像、初始状态、随机种子、最多 8 轮预算、状态诊断器和验证机会，学习变化函数不读取方法名称，并对重复教学动作施加相同的边际收益递减。固定 Skill 的迁移率仍可能高于自适应方法；当前模拟中自适应方法的优势主要体现在标准化增益、单位轮次效率和决策质量。主动展示这一 trade-off 比声称“全面领先”更可信。

### 为什么保留失败案例？

牛顿第一定律案例虽然后测掌握较高，但弱基础学生首次迁移没有通过，系统在 8 轮后仍判未成功。它证明终止守卫不会为了结果好看而把“当前题进步”冒充“可迁移掌握”，也提示后续要优化轮次预算分配。

### 评估结果能代表真实教学效果吗？

不能直接替代真实学生实验。固定种子模拟适合策略消融、回归测试和复现；真实部署还需要教师盲评、真实学生实验、延迟测验和模型方差分析。界面与报告均主动标明这一边界。
