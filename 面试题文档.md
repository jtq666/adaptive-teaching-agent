# 自适应教学 Agent 面试题文档

## 一、项目定位

### 1. 你的项目解决什么问题？

项目解决的是“普通问答不能持续适应学生状态”的问题。系统根据学生每一轮真实回答，判断当前困难并改变教学动作，同时保存掌握度和证据。

### 2. 项目的核心创新是什么？

一次真实 LLM 调用同时完成学生理解判断、困难识别、教学动作选择和教师回复生成。程序不再拆成多个互相覆盖的策略决策模块。

### 3. 内容 Skill 和教学动作有什么区别？

内容 Skill 表示当前教什么，例如牛顿第一定律或导数极限定义；教学动作表示怎么教，例如讲解、诊断、分层提示、误解纠正和迁移。

## 二、关于模型和硬编码

### 4. 这是不是把学生句子和 Skill 写死了？

不是。演示案例和内容 Skill 是预先确定的输入范围，但学生回答后的困难类型、教学动作和回复由真实模型生成。代码没有把某个固定句子直接映射成某个动作。

### 5. 程序还保留哪些控制？

程序只做最基本的安全边界：

- 检查内容 Skill 是否存在；
- 保存模型返回的状态；
- 记录调用审计；
- 防止非法输出影响会话；
- 达到掌握条件或最大轮次时结束。

### 6. 为什么只保留五种教学动作？

动作太多会让多个策略模块互相干预。五种动作已经覆盖现场教学的主要行为，同时让模型有足够空间组织具体语言。

## 三、关于两个案例

### 7. 物理案例如何体现自适应？

学生先表达困惑，模型可以解释或诊断；学生明确说“运动必须有力维持”后，才进入误解纠正；学生修正后，再通过公交车加速、转弯和电梯场景验证迁移。

### 8. 导数案例为什么连续使用 scaffold？

学生说“这个不会”“我看不懂”时，只能确认其遇到困难，不能确认他坚持了错误概念。因此合理策略是逐步拆小任务，而不是直接纠错或给出完整公式。

### 9. 为什么看起来内容 Skill 不变？

因为内容目标没有改变。变化的是教学动作、困难类型、掌握度和教师回复。内容 Skill 负责保持学科主题稳定，避免教学过程中跑题。

## 四、关于评估和可靠性

### 10. 项目如何评估？

评估包含 18 个案例、三种教学方法和多种学生画像与随机种子，观察决策质量、教学行为、状态更新、迁移和调用稳定性。

### 11. 评估结果能否说明真实教学有效？

不能直接等同。当前评估验证的是 Agent 的决策逻辑和交互契约，真实课堂效果还需要人工盲评和真实学生样本。

### 12. API 失败时怎么办？

应用有兼容回退，避免会话数据丢失；但正式物理和导数演示脚本会检查回退标志，发生回退就报错，不会把离线结果冒充真实 API 结果。

### 13. 如何证明每轮不是多个模型调用互相干预？

每轮保存 generation_audit，记录单次自适应教学调用、模型动作和调用次数。两个演示脚本也会检查每轮调用数量。

## 五、关于项目取舍

### 14. 为什么没有继续保留很多策略 Skill？

旧策略文件会造成流程重复和 Skill 乱跳。当前实时链路使用统一的自适应教学 Skill，历史数据和评估兼容逻辑与实时教学决策分开。

### 15. 为什么演示时还要选择内容 Skill？

因为面试案例已经确定教学主题。选择内容 Skill 只是限定“教什么”，并没有指定“怎么教”；教学动作仍由模型根据学生回答自主决定。

### 16. 如果每次模型回复不完全一样，如何验收？

不要求文字完全一致，主要检查行为是否正确：是否承接学生原话、是否选择合适动作、是否保持当前知识目标、是否只提出一个问题、是否在掌握后进行迁移。

## 六、最后可以这样总结

> 这个项目的重点不是把教学流程写成越来越多的规则，而是让模型真正理解学生当前的困难并生成下一步教学。程序只负责状态、合法性和终止边界，因此同一套框架既可以用于公交车惯性，也可以用于导数的符号学习。
