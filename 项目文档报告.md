# 自适应教学 Agent 项目报告

**华东师范大学夏令营硕士生综合考察题（二）**

项目仓库：[https://github.com/jtq666/adaptive-teaching-agent](https://github.com/jtq666/adaptive-teaching-agent)

## 一、项目简介

本项目实现了一个面向实时教学的自适应教师 Agent。每个实时回合只调用一次真实 LLM，同时完成学生理解判断、内容 Skill 选择、教学动作选择和教师回复生成；程序只校验内容 Skill、保存状态并展示结果。

项目要解决的核心问题是：让 Agent 能够根据学生状态动态调整教学方式，而不是对所有学生使用同一套固定问答。

## 二、系统工作流程

```text
学生回答
   ↓
一次自适应教学模型调用
   ↓
内容 Skill + 教学动作 + 困难 + 证据 + 教师回复
   ↓
程序保存状态并进入下一轮
```

系统重点遵循三条原则：

1. 内容 Skill 与教学动作分开记录，但不由多个策略模块重复决策。
2. 教学动作由模型从 `explain / diagnose / scaffold / correct / transfer` 中自主选择。
3. 程序只负责 Skill 合法性、状态持久化和最大轮次边界，不用关键词或连续失败次数覆盖模型判断。

## 三、系统演示

演示主题为“公交车急刹车与惯性”。截图来自真实浏览器验收，使用 Streamlit 页面和已配置的 LLM 完成。

### 1. 实时教学：围绕学生回答继续追问

学生提交真实回答后，Agent 用一次 API 生成下一轮教学，页面展示内容 Skill、教学动作和简短证据。

![实时教学页面](docs/screenshots/01-live-teaching.png)

### 2. 教学动作变化：模型选择分层提示

例如导数案例中，学生说“这个不会”“我看不懂”时，模型连续选择 `scaffold`，保持原情境并逐步降低任务粒度。

![教学动作变化与决策证据](docs/screenshots/02-skill-switch.png)

### 3. 过程回放：保存每轮教学状态

系统保存教学轮次、掌握度变化、Skill 切换次数和会话状态，支持继续教学、复制和归档。

![过程回放页面](docs/screenshots/03-session-replay.png)

### 4. Agent 评估：查看方法对比和导出结果

评估页面支持快速演示、完整评估、结果统计、八维盲评表和 Markdown/CSV/JSON 导出。

![Agent 评估页面](docs/screenshots/04-evaluation.png)

## 四、项目主要功能

- **实时教学**：学生每次提交一条回答，Agent 再决定下一步教学动作。
- **Teaching Skill Library**：保留学科内容 Skill；实时教学使用一个统一的 `adaptive_teaching_v1`，内部动作只有五种。旧策略文件仅用于历史兼容。
- **动态决策**：模型根据学生原话、历史上下文和当前目标自主选择教学动作，程序不二次覆盖。
- **模型自主教学**：一次输出同时包含理解判断、困难类型、证据、掌握度和面向学生的回复。
- **过程回放**：保存对话、掌握度、误解证据、Skill 切换和模型调用记录。
- **评估实验室**：对比自适应混合 Agent、固定单 Skill Agent 和通用 Agent。
- **可复现实机演示**：物理和导数各有一套真实 API 输入脚本，分别验证误解纠正/迁移和连续分层提示。

## 五、评估与测试结果

全量评估包含 **18 个测试案例、810 个评估单元**。结果如下：

| 方法 | 决策质量 | 标准化学习增益 | 迁移正确率 |
|---|---:|---:|---:|
| 自适应混合 Agent | 80.5% | 0.352 | 25.2% |
| 固定单 Skill Agent | 66.0% | 0.326 | 20.4% |
| 无 Skill 通用 Agent | 33.3% | 0.145 | 7.0% |

工程验证结果：

- 核心结构回归、页面回归和真实 API 专项验收均已执行；不把测试数量作为教学设计约束。
- Ruff 静态检查：通过。
- Mypy 类型检查：通过。
- 真实浏览器验收：通过，覆盖实时教学、Skill 切换、过程回放、评估和响应式页面。

这里的全量评估主要验证 Agent 的决策逻辑和交互契约，不等同于真实课堂中的长期学习效果；真实教学效果仍需要后续人工盲评或学生样本验证。

本轮重点确认了两个演讲案例：导数脚本真实路径为 `scaffold → scaffold → scaffold`；物理脚本在学生明确断言“运动必须有力维持”后进入 `correct`，随后进入 `transfer` 并成功终止。每个实时回合均为一次 API 调用，回复不超过 100 字。

两套现场脚本及脱敏结果位于 `scripts/demo_physics.py`、`scripts/demo_derivative.py` 和 `output/evaluations/demo_*_run.json`，可按文档逐条输入复现。

## 六、运行方式

在项目根目录执行：

```bash
pip install -r requirements.txt
streamlit run streamlit_app.py
```

如果使用真实 LLM，需要在 `.env` 中配置对应的 API 信息。程序入口为 `streamlit_app.py`，打开后可以看到“实时教学、Skill Library、过程回放、Agent 评估”四个页面。

## 七、项目总结

本项目的核心成果是形成了一个可运行、可解释、可评估的教学闭环：

**学生回答 → 一次 LLM 自适应输出 → 状态保存与展示 → 下一轮教学。**

相比普通的一次性 AI 问答，系统能够保留教学上下文，识别学生困惑，并根据学生反应改变下一步教学方式。
