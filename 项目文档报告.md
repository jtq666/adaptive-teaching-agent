# 基于 Teaching Skill Library 的自适应教学 Agent

**华东师范大学夏令营硕士生综合考察题（二）项目报告**

项目仓库：<https://github.com/jtq666/adaptive-teaching-agent>

---

## 1. 项目背景与目标

本项目的目标不是一次性生成完整教案，而是构建一个能够进行实时、多轮交互的教师 Agent。

系统根据教学目标、学生画像、初始掌握状态、历史对话和可用 Teaching Skill，判断学生当前最需要什么帮助，然后只输出下一步教学动作，等待学生回答后再继续决策。

项目核心闭环为：

```text
学生回答
  ↓
理解当前回答与困惑
  ↓
更新学生状态
  ↓
选择或切换教学 Skill
  ↓
先给针对性反馈，再提出一个问题
  ↓
推进知识点或进入迁移验证
```

项目重点验证三项能力：

1. Agent 能否显式维护学生掌握情况、误解、理解信号和下一关注点；
2. Agent 能否根据学生反馈动态选择、组合和切换 Teaching Skill；
3. Agent 能否通过教学前后状态、迁移验证和基线对比评估教学过程。

---

## 2. 需求理解

原题要求教师 Agent：

- 输入教学目标、学生画像、当前掌握状态、历史对话和 Skill 列表；
- 每次只输出一个下一步教学动作，等待真实学生回答；
- 根据学生反馈选择、组合和切换 Teaching Skill；
- 显式维护掌握情况、误解、理解信号和下一关注点；
- 在教学成功或判断无法继续时终止；
- 使用测试样例和至少一个基线方法进行可复现评估。

本项目保留上述要求，但没有把实现限定为固定的多角色或复杂提示链。实时教学采用更简单的“反馈优先”设计：

```text
先回应学生刚才的问题
→ 澄清当前最关键的一个误解
→ 只提出一个可回答的问题
→ 根据回答推进或切换教学策略
```

这样既满足“每次只输出下一步动作”的要求，也避免把教学过程退化为连续的证据收集或重复追问。

---

## 3. 系统设计

### 3.1 实时教学主链路

实时教学对外由一个 `HybridTeachingAgent` 负责，内部统一维护 `TeachingSession`。

核心流程如下：

```text
创建会话
  ├─ 教学目标
  ├─ 学生画像
  ├─ 初始掌握状态
  ├─ 历史对话
  └─ 可用 Skill
       ↓
生成课程路线
       ↓
教师输出首个问题
       ↓
学生提交回答
       ↓
状态诊断与证据更新
       ↓
简单教学流：反馈 → 一个问题
       ↓
Skill/策略切换、路线推进或迁移验证
```

实时教学默认启用简化模式，主要教学动作包括：

| 动作 | 使用场景 |
|---|---|
| 学科讲解 | 学生理解方向基本正确，继续围绕当前知识点教学 |
| 诊断 | 学生表达困惑、无法回答或回答方向不清晰 |
| 分层提示 | 学生连续受阻，需要降低回答难度 |
| 误解纠正 | 学生持续坚持与当前概念冲突的解释 |
| 迁移验证 | 当前知识点已有足够证据，需要在新情境中验证 |

与学生直接可见的回复采用“短反馈 + 一个问题”的形式。反馈可以解释学生刚才的困惑，但不会一次性生成完整多轮对话。

### 3.2 学生状态模型

系统显式保存以下状态：

- `mastery`：逐知识点掌握度；
- `misconceptions`：当前识别出的误解；
- `misconception_states`：误解持续次数和关联知识点；
- `understanding_signals`：当前轮次的理解信号；
- `next_focus`：下一步教学关注点；
- `evidence`：学生原回答、知识点、证据等级和判断依据；
- `transfer_verified`：迁移验证是否通过。

掌握度是基于可观察回答证据的估计，不被表述为学生真实能力概率。证据等级包括：

```text
none → partial → correct → explained → transfer
```

### 3.3 Teaching Skill

系统将第一题得到的学科 Skill 保存在 `data/skills/`，包括：

- 高等数学：导数极限定义、变上限积分等；
- 大学物理：牛顿第一定律、牛顿第二定律、牛顿第三定律、动量守恒等；
- 程序设计：二分查找边界条件。

第二题补充了跨学科教学策略 Skill：

- `diagnostic_questioning_v1`：基于回答证据定位学生当前理解；
- `scaffolded_hint_ladder_v1`：学生受阻时逐层降低难度；
- `misconception_contrast_correction_v1`：通过对比和反例纠正误解；
- `transfer_verification_v1`：通过新情境验证是否能够迁移。

内容 Skill 负责“教什么”，策略 Skill 负责“怎么教”。每轮会话记录当前 Skill、选择理由、候选 Skill 和切换原因。

### 3.4 终止机制

系统不会只因为学生给出一句正确话就立即结束。成功终止需要同时满足：

- 各目标知识点达到掌握阈值；
- 存在解释性证据；
- 学生通过独立迁移验证。

如果达到最大轮数、纠错后仍无改善，系统会暂停会话并保留当前状态，允许之后从原路线继续。

---

## 4. 系统功能

### 4.1 实时教学

- 输入课程、主题、教学目标和知识点；
- 选择学生水平、先备知识和初始掌握度；
- 支持历史回答导入和 Skill 限制；
- 开放式回答实时提交；
- 展示教师反馈、当前教学动作、Skill 和路线进度；
- 支持学生视图与教师/答辩视图切换。

### 4.2 Skill Library

- 浏览和搜索学科 Skill；
- 按课程和类型筛选；
- 导入 YAML Skill 并进行字段校验；
- 冲突时生成新版本，不静默覆盖内置 Skill；
- 导出单个 Skill；
- 内置 Skill 只读，用户 Skill 独立存储。

### 4.3 过程回放

- 保存完整会话 JSON；
- 展示逐轮学生回答、教师反馈、状态变化和 Skill 切换；
- 支持搜索、分页、展示名编辑、复制、归档、删除和恢复；
- 支持继续教学并保留原会话 ID、历史对话和课程路线。

### 4.4 Agent 评估

- 快速评估：18 个案例 × 3 种方法，共 54 个方法单元；
- 完整评估：18 个案例 × 3 种学生画像 × 5 个随机种子 × 3 种方法，共 810 个方法单元；
- 自动生成 JSON、CSV、Markdown、盲评材料和盲评密钥；
- 支持评估历史、归档、导入、导出和回收站恢复。

---

## 5. 评估方案

### 5.1 数据集

评估案例由开发集和冻结留出集组成：

| 数据集 | 案例数 | 作用 |
|---|---:|---|
| 开发集 | 6 | 开发和回归测试 |
| 留出集 | 12 | 检验新情境下的泛化 |
| 合计 | 18 | 完整评估范围 |

三门课程各包含 6 个案例，覆盖高等数学、大学物理和程序设计。

### 5.2 对比方法

| 方法 | 说明 |
|---|---|
| 自适应混合 Agent | 根据学生状态选择学科 Skill 和教学策略，并动态切换 |
| 固定单 Skill | 全程使用单一学科 Skill，保留统一验证机会 |
| 无 Skill 通用 Agent | 不使用 Skill Library，仅使用通用教学回复 |

三种方法共享相同案例、初始状态、学生画像、随机种子、轮次预算和迁移阈值。

### 5.3 评估指标

评估覆盖四类指标：

1. **学生状态判断**：误解 Precision、Recall、F1、掌握度 MAE、下一关注点准确率；
2. **教学决策质量**：Skill 选择准确率、Skill 切换准确率、终止准确率；
3. **教学行为质量**：知识正确性、清晰度、针对性、促进思考、不直接给答案、上下文连贯、教师语气和可执行性；
4. **教学效果**：前后测分数、标准化学习增益、单位轮次效率、迁移正确率和成功率。

自动行为分明确标注为 proxy，只用于策略回归和方法对照，不冒充真实课堂效果。

### 5.4 最新完整评估结果

最新 full 评估包含 810 个方法单元：

| 方法 | 决策质量 | 标准化增益 | 迁移正确率 | 单步契约 | 上下文连续 |
|---|---:|---:|---:|---:|---:|
| 自适应混合 Agent | 0.797 | 0.343 | 0.244 | 1.000 | 1.000 |
| 固定单 Skill | 0.660 | 0.326 | 0.204 | 0.000 | 0.000 |
| 无 Skill 通用 Agent | 0.333 | 0.145 | 0.070 | 0.000 | 0.000 |

自适应 Agent 在当前冻结模拟中的决策质量、标准化增益和迁移率高于两个基线。评估同时保留失败案例，避免只展示成功结果。

评估证据文件位于：

- `output/evaluations/evaluation_*.json`：完整结构化结果；
- `output/evaluations/evaluation_*.csv`：方法汇总；
- `output/evaluations/evaluation_*.md`：可读报告；
- `output/evaluations/annotation_blind_*.csv`：人工盲评材料；
- `output/evaluations/annotation_key_*.csv`：盲评对应关系。

人工双人盲评需要真实评审员参与，项目不会用自动分数伪造人工结果；当前材料已经生成，人工标注仍作为独立后续步骤。

---

## 6. 典型教学过程

以“公交车急刹车解释惯性”为例：

### 第一步：现象预测

教师提问学生刹车时身体如何运动。学生回答身体会继续向前移动，系统记录对惯性现象的正确证据。

### 第二步：学生出现困惑

学生追问：“公交车受到向后的力，那我的身体到底有没有受到力？”

系统先澄清：身体可能受到重力和支持力，但没有受到足以使其立即减速的向后水平力；然后只提出一个关于水平合力的问题。

### 第三步：概念解释

学生说明惯性不是一种力，而是保持原有运动状态的性质。系统据此推进到合力与运动状态的关系。

### 第四步：迁移验证

系统切换到新的情境，要求学生说明如果公交车突然启动，身体相对车会如何运动，并解释改变运动状态的原因。

这条过程体现了：

```text
现象预测 → 困惑澄清 → 概念区分 → 新情境迁移
```

而不是：

```text
判断依据 → 具体事实 → 事实如何支持判断
```

---

## 7. 技术实现

| 模块 | 技术 | 说明 |
|---|---|---|
| 交互界面 | Streamlit 1.59 | 实时教学、Skill Library、回放和评估四页应用 |
| Agent | Python | 统一会话编排和简化实时教学流 |
| 结构化模型 | Pydantic v2 | 会话、状态、Skill、路线和评估报告校验 |
| LLM | OpenAI-compatible API | 状态诊断、反馈生成和教学问题生成 |
| 配置 | YAML + `.env` | 模型、路径、轮次和评估配置 |
| 持久化 | JSON | 会话、状态证据和评估报告本地保存 |
| 测试 | pytest + Streamlit AppTest | 业务、组件、页面和回归测试 |
| 质量检查 | Ruff + mypy + quality gate | 代码规范、类型检查和交付一致性 |

项目入口：

```powershell
cd 考核2
python -m pip install -r requirements.txt
streamlit run streamlit_app.py
```

离线回归：

```powershell
python -m pytest -q
python scripts/quality_gate.py
python scripts/run_evaluation.py --mode quick
python scripts/run_evaluation.py --mode full
```

真实 API 验收：

```powershell
python scripts/online_multicase_acceptance.py --trials 1 --retries 2
python scripts/online_physics_route_acceptance.py
python scripts/online_progression_acceptance.py
```

---

## 8. 验收结果

当前项目已完成：

- 自动化测试 `174 passed`；
- Ruff 检查通过；
- mypy 检查通过；
- 质量门禁通过：18 个案例、174 项测试、覆盖率门槛 92%；
- full 评估 810 个方法单元已生成；
- 提交包已生成并通过 `.env`、缓存和字节码排除检查；
- 会话、Skill、评估报告的主要 CRUD 和导出功能已覆盖；
- 实时教学、过程回放、Skill Library 和 Agent 评估页面可运行。

最终提交包：

```text
release/exam2-submission-20260816-180932.zip
```

---

## 9. 项目边界与后续工作

当前评估属于可复现策略仿真，适合比较教学决策、回归系统行为和验证工程约束，但不能替代真实学生实验。

后续如果继续研究，可以开展：

1. 教师双人盲评和一致性分析；
2. 真实学生小规模对照实验；
3. 延迟测验和迁移保持度测试；
4. 不同模型、温度和提示版本的稳定性分析；
5. 更长时间的个性化教学轨迹研究。

---

## 10. 总结

第一题解决的是：如何从教学材料中提取结构化、可执行的 Teaching Skill。

第二题解决的是：面对一个具体学生，下一轮应该选择什么教学动作，以及如何根据学生反馈动态调整。

本项目形成了从 Skill Library、学生状态建模、实时交互、动态 Skill 切换、迁移验证到可复现评估的完整闭环。
